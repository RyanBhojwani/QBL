[build]
builder = "nixpacks"

[deploy]
# -u = unbuffered stdout so logs appear in Railway immediately
startCommand = "PYTHONPATH=/app/Original_Code python -u /app/Original_Code/bet_scheduler7.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
