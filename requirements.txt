# Quant Bet Labs — Python worker dependencies
# Pin supabase and storage3 together — newer storage3 requires pyiceberg (needs MSVC)
supabase==2.18.1
storage3==0.12.1

# Core data
pandas>=2.0
numpy>=1.24
pyarrow>=14.0

# ML models (loaded from models/ directory)
scikit-learn>=1.3
lightgbm>=4.0
scipy>=1.11
statsmodels>=0.14
joblib>=1.3

# Discord
discord.py>=2.3
aiohttp>=3.9

# Odds API / HTTP
requests>=2.31

# Timezone data — required for zoneinfo on Linux (Railway)
tzdata>=2024.1
